<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme (legacy / fallback) -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Main runtime theme (used after splash) -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/status_orange</item>
        <item name="android:windowTranslucentStatus">false</item>
    </style>

    <!-- Launch / Splash theme (your manifest points here) -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Your existing splash drawable -->
        <item name="android:background">@drawable/splash</item>

        <!-- IMPORTANT: switch to the real theme after the splash screen -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

</resources>